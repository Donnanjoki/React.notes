// End of Chapter 2: Book>> Learning React

/*
Rules to follow:
A] Keep the data immutable
B] Keep functions pure - accept at least one argument, return data or another function
C] Use Recursion over looping (wherever possible)
*/

/*
Challenge: Building a Ticking Clock
#The clock needs to display hours, minutes, seconds and time of day in a civilian time
#Each field must have double digits, meaning leading zeros need to be applied to single digit values
like 1 or 2.
#The clock must tick and change  the display every second.

*/
